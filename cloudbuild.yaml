steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/rampancy-assault-corps-server'
      - '-f'
      - 'rampancy_assault_corps_server/Dockerfile'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/rampancy-assault-corps-server'
